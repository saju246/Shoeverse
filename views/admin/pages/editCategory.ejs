<!-- < include('../includes/header') %> -->


  <div class="container">
    <div class="row mt-5">
      <div class="col-md-6 offset-md-3 center-content">
        <h1>Edit Category</h1>
        <form action="" method="post" onsubmit="return categoryValidation()">
          <div class="form-group text-center">
            <label for="categoryName" class="mt-2">Name:</label>
            <div class="col-md-6 mx-auto">
              <input type="text" name="updatedName" class="form-control" value="<%= value.categoryName %>" id="name"
                required />
            </div>
            <div id="nameerr" class="text-danger fw-bolder"></div>
          </div>

          <!-- <div class="form-group text-center">
            <label for="categoryName" class="mt-2">Offer Percentage:</label>
            <div class="col-md-6 mx-auto">
              <div class="input-group">
                <input type="number" class="form-control" id="offer" name="offer"
                  value="= value.offer ? value.offer : '' %>" />
                <div class="input-group-append">
                  <span class="input-group-text">%</span>
                </div>
              </div>
              <div id="offerr" class="text-danger fw-bolder"></div>
            </div>
          </div> -->
<!-- 
          <div class="form-group text-center">
            <label for="startDate" class="mt-2">Start Date:</label>
            <div class="col-md-6 mx-auto">
              <input type="date" class="form-control" id="startDate" name="startDate"
                value="< value.startDate ? value.startDate.toISOString().split('T')[0] : '' %>" />
            </div>
          </div> -->

          <!-- <div class="form-group text-center">
            <label for="endDate" class="mt-2">End Date:</label>
            <div class="col-md-6 mx-auto">
              <input type="date" class="form-control" id="endDate" name="endDate"
                value="< value.endDate ? value.endDate.toISOString().split('T')[0] : '' %>" />
            </div>
            <div id="dateerr" class="fw-bolder text-danger"></div>
          </div> -->

          <!-- <div class="form-group text-center">
            <label for="description" class="mt-2">Description:</label>
            <div class="col-md-6 mx-auto">
              <textarea class="form-control" id="description" rows="1"
                name="description">< value.description ? value.description : '' %></textarea>
            </div>
          </div> -->

          <button class="btn btn-submit" type="submit">Update</button>
        </form>
      </div>
    </div>
  </div>

  <%- include('../includes/flashMessage') %>

    <script>
      const message = document.getElementById("message");

      const hide = () => {
        message.style.display = "none";
      };

      setTimeout(hide, 3000);
    </script>

    <style>
      /* Style to add spacing from the top navbar */

      /* Center the content */
      h1 {
        color: #fff;
      }

      .center-content {
        text-align: center;
        margin-top: 50px;
        /* Adjust the margin-top as needed */
        padding-top: 20px;
        /* Adjust the padding-top as needed */
        background-color: #a9a8a8;
        color: #fff;
        border: #000000;
        box-shadow: #000000 1px 2px rgba(0, 0, 0, 0);
      }

      /* Style for the input and button */
      .form-group {
        margin-bottom: 10px;
        /* Adjust the margin-bottom as needed */
      }

      .form-group label {
        display: block;
        font-weight: bold;
      }

      .input-group input::placeholder {
        color: #ffffff;
      }

      /* Style for the button */
      .btn-submit {
        background-color: #ffffff;
        color: #000000;
        border: none;
      }
    </style>
    <script src="/admin/js/categoryValidation.js"></script>


    <!-- <include('../includes/footer') %> -->